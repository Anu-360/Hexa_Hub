using System;

public class Enum
{
    public enum AssetStatus
    {
        OpenToRequest,
        Allocated,
        Maintenance
    }
    public enum UserType
    {
        Employee,
        Admin
    }
}
